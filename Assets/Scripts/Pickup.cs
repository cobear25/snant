using UnityEngine;

public class Pickup : MonoBehaviour
{
    // 0 produces a worker, 1 produces a soldier
    public int type = 0;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
